spring:
  application:
    name: application-service
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: hr
    password: hussam
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          fetch_size: 100

server:
  port: 8084

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

services:
  job-service:
    url: ${JOB_SERVICE_URL:http://localhost:8082}
  candidate-service:
    url: ${CANDIDATE_SERVICE_URL:http://localhost:8083}
  notification:
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8086/api/notifications/send-email}